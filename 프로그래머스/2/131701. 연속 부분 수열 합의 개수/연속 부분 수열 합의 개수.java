class Solution {
    public int solution(int[] elements) {
        HashSet<Integer>[] set = new Hashset[elements.length];
    
        for (int i = 0; i < elements.length; i++) {
            set[i] = new HashSet<>();
            for (int j = 0; j < elements.length; j++) {
                set[i].add
            }
        }
        int answer = 0;
        return answer;
    }
}